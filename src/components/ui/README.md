# components/ui

Componentes de base gerados/adaptados pelo shadcn/ui (sobre Radix UI). Estilizados com os tokens do Design System (ver `src/styles/tokens.css`). Não colocar aqui componentes de feature específicos de uma página — só primitivos reutilizáveis (Button, Input, Card, etc).
