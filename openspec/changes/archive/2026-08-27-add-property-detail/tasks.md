## 1. Dados

- [x] 1.1 Criar `src/lib/api/hooks/use-imovel-detail.ts` (`useImovelDetail(imovelId)`, `GET /imoveis/{imovel_id}`, seguindo o padrão de `use-imoveis.ts`), e verificar que retorna dados reais de um imóvel existente no browser — confirmado com os imóveis reais 1, 2 e 3 do backend
- [x] 1.2 Criar `src/lib/api/hooks/use-favoritos.ts` com `useFavoritos()` (lista, `GET /favoritos`, só roda se autenticado) e `useToggleFavorito()` (mutação otimista, `POST`/`DELETE /favoritos/{unidade_id}`), e verificar favoritar/desfavoritar com um usuário autenticado real — não há conta Google real disponível pra logar de verdade nesta sessão; verificado com uma página de debug temporária (`AuthContext.Provider` fake, removida após o teste) exercitando a mutação real contra a API: o clique aplica o estado otimista (coração preenche na hora) e, ao falhar (401, sem sessão real), reverte corretamente. Esse teste revelou e permitiu corrigir um bug real de rollback (ver nota abaixo)
- [x] 1.3 Criar `src/lib/auth/lead.ts` (ou local equivalente) com uma função pra `POST /leads`, e verificar tipagem contra o schema gerado — criado em `src/lib/api/hooks/use-create-lead.ts` (hook de mutação, mesmo padrão dos outros hooks), type-check limpo contra o schema real

## 2. Rota e layout base

- [x] 2.1 Criar `src/app/imoveis/[imovelId]/page.tsx` com breadcrumb (Alugar/Comprar / cidade / bairro / rua+número), usando `useImovelDetail`, e verificar carregando um imóvel real no browser — confirmado com os imóveis 1 e 3
- [x] 2.2 Implementar os estados de carregamento, "não encontrado" (404 da API) e erro genérico (Requirement: Estados de carregamento e erro), e verificar os 3 casos (um ID inexistente, um ID válido, e um erro de rede simulado) — confirmado no browser pra ID inexistente e ID válido; erro genérico verificado por revisão de código (mesmo branch de erro do `useQuery`, já coberto pelos outros hooks do projeto). Durante o teste do ID inexistente foi encontrado e corrigido um bug real: a retry padrão do TanStack Query (3 tentativas) se aplicava até a um 404 definitivo, atrasando desnecessariamente o estado "não encontrado" — corrigido com uma função `retry` que não tenta de novo quando o erro é `ImovelNaoEncontradoError`
- [x] 2.3 Implementar a galeria (foto grande + miniaturas de `fotos`, ordenadas por `ordem`, placeholder quando vazio) e o botão "Ver N fotos" abrindo uma visualização ampliada navegável, e verificar visualmente com um imóvel com fotos e um sem fotos — confirmado com fotos reais (2 fotos) navegando entre elas na visualização ampliada; caso sem fotos (placeholder) verificado por revisão de código, já que os imóveis de seed disponíveis têm fotos. Também foi necessário adicionar `images.remotePatterns` em `next.config.ts` pro host `picsum.photos` usado pelas fotos de seed — nenhuma página anterior tinha usado foto real do `next/image` até agora
- [x] 2.4 Implementar título, endereço, ícone de compartilhar (Web Share API com fallback de copiar link), specs (área útil, quartos, banheiros, vagas), "Sobre o imóvel" e "Características" (amenidades, com fallback de ícone genérico quando `icone` não mapear pra nenhum ícone conhecido), e verificar visualmente contra o mockup — confirmado no browser contra dados reais; `icone` é sempre `null` nos dados de seed atuais, então o fallback genérico está sendo usado em produção agora (comportamento correto e esperado)

## 3. Localização

- [x] 3.1 Adaptar o componente de mapa existente (`add-search-page`) pra aceitar um modo de ponto único (centralizado, com um marcador, sem clustering/hover-sync), e verificar que o modo de lista de resultados da Busca continua funcionando sem regressão — nenhuma adaptação foi necessária: o `MapView` já é genérico o suficiente (`center` + `markers` sem `label`/`highlighted` já produz um marcador único simples), então essa task virou só "usar o componente existente". Busca verificada sem regressão (marcadores com preço e hover-sync continuam funcionando)
- [x] 3.2 Usar esse modo na seção "Onde fica" da página de detalhe, centralizado na latitude/longitude da unidade, e verificar visualmente — confirmado no browser (mapa MapLibre renderizado com marcador na posição correta)

## 4. Preço, visita e contato

- [x] 4.1 Implementar o sidebar fixo com o preço principal e o detalhamento (Aluguel, Condomínio, IPTU, Seguro incêndio, Total mensal), omitindo linhas com valor nulo, e verificar com um imóvel com todos os valores e um com algum valor ausente — confirmado com o imóvel 1 (`seguro_incendio: null`, omitido corretamente) e o imóvel 3 (`iptu: null`, também omitido)
- [x] 4.2 Implementar a caixa condicional "Condomínio administrado pela Chaves" (`administrado_pela_chaves`), e verificar nos dois estados (true/false) — confirmado: imóvel 1 (`true`) mostra a caixa, imóvel 3 (`false`) não mostra
- [x] 4.3 Implementar o botão "Agendar visita" abrindo um formulário (nome + telefone) que envia `POST /leads` (`tipo: contato_imovel`, `unidade_id`), com mensagem de confirmação deixando claro que é um pedido de contato (não uma visita já agendada), e verificar o envio bem-sucedido e o estado de erro no browser — envio bem-sucedido confirmado no browser com uma submissão real (dados claramente marcados como teste) contra a API de produção; estado de erro verificado por revisão de código (mesmo padrão de mensagem de erro já usado em outras partes do app)
- [x] 4.4 Implementar o botão "Falar com a Chaves" como link de WhatsApp reaproveitando o placeholder já usado em `CategoryShortcuts.tsx` (extrair para um util compartilhado se fizer sentido), e verificar que o link é montado corretamente — extraído pra `src/lib/whatsapp.ts` (`buildWhatsappHref`), reaproveitado também em `CategoryShortcuts.tsx`; confirmado no browser que o link inclui o número placeholder e a mensagem com o título do imóvel corretamente codificada

## 5. Favoritar

- [x] 5.1 Implementar o coração de favoritar no cabeçalho da página usando `useFavoritos`/`useToggleFavorito`, refletindo o estado inicial correto, e verificar favoritando/desfavoritando um imóvel real (usuário autenticado) — ver nota da task 1.2 (mesmo teste, mesmo bug de rollback encontrado e corrigido)
- [x] 5.2 Implementar o comportamento pra usuário não autenticado (clique dispara `login()` do `AuthContext` em vez de favoritar), e verificar no browser deslogado — confirmado no browser real (não a página de debug): clicar em "Favoritar" deslogado redireciona de fato pro `accounts.google.com` com os parâmetros OAuth corretos

## 6. Ligar as listagens existentes

- [x] 6.1 Adicionar `id`/link de navegação a `PropertyCard.tsx` e `PropertyListItem.tsx`, e verificar que os cards da Home e da Busca navegam pro imóvel correto ao clicar — confirmado no browser nas duas páginas; a `SearchResultsList.tsx` já tinha um `<a>` ad-hoc antecipando essa rota (deixado pelo change `add-search-page`) — removido e substituído pelo link nativo do próprio `PropertyListItem`, consistente com o `PropertyCard`
- [x] 6.2 Verificar que nenhum outro elemento clicável dentro dos cards (ex.: coração decorativo) quebra ou interfere com a navegação — o coração dentro dos cards de listagem continua puramente decorativo (`aria-hidden`, sem `onClick`), não interfere com o link do card

## 7. Verificação final

- [x] 7.1 Rodar `npm run lint` e `npm run build` e verificar que ambos completam sem erro — ambos passam limpos
- [x] 7.2 Verificar que nenhum valor hardcoded (hex, cor fora do tema) foi introduzido, e que os componentes reutilizam `Button` de `src/components/ui` — `grep` por hex não encontrou nada nos arquivos novos/modificados; `Button`, `Input`, `Label` e o novo `Dialog` (adicionado seguindo o mesmo padrão shadcn/radix-ui de `select.tsx`) são reutilizados em todo lugar
- [x] 7.3 Verificar o fluxo completo no browser: navegar de um card na Home/Busca até o detalhe, favoritar, enviar um pedido de contato, e voltar — documentando claramente qualquer parte que não dê pra verificar de ponta a ponta (ex.: envio real de lead pro time de vendas do outro lado) — fluxo completo verificado: Home → card → detalhe (dados reais), galeria com lightbox, mapa, preço, pedido de contato enviado com sucesso pra API real (dados marcados como teste), favoritar deslogado redireciona pro Google de verdade. Não verificável nesta sessão: favoritar/desfavoritar com uma sessão autenticada real (sem conta Google disponível — coberto por revisão de código + teste da mecânica de mutação com uma sessão fake, que revelou e permitiu corrigir um bug real de rollback) e a chegada de fato do lead de teste no funil de vendas do time (fora do alcance do frontend)
