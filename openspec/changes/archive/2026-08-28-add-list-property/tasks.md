## 1. Dados

- [x] 1.1 Criar `src/lib/api/hooks/use-geocode-address.ts` (`useGeocodeAddress()`, mutação que chama `https://nominatim.openstreetmap.org/search?q=...&format=json&limit=1`), e verificar no browser com um endereço real, incluindo o caso de nenhum resultado — confirmado com "Avenida Paulista, 1000, Bela Vista, São Paulo" (resultado real, endereço resolvido corretamente) e com um endereço inventado (retornou array vazio, mapeado pra `null`)
- [x] 1.2 Criar `src/lib/api/hooks/use-create-imovel.ts` (`useCreateImovel()`, mutação `POST /imoveis`), e verificar tipagem contra o schema gerado — type-check limpo contra o schema real

## 2. Página e formulário

- [x] 2.1 Criar `src/app/anunciar/page.tsx`: `status !== "authenticated"` → carregamento ou dispara `login()`, sem renderizar o formulário, e verificar no browser (mesmo padrão de `/favoritos`) — confirmado no browser real (deslogado): acessar `/anunciar` redireciona de fato pro `accounts.google.com`, sem mostrar o formulário
- [x] 2.2 Implementar os campos de endereço, specs físicos, título, descrição, tipo de operação (aluguel/venda) e valores comerciais condicionais, com validação de que pelo menos um tipo de operação está marcado antes de permitir envio, e verificar no browser — confirmado: os campos de valores de aluguel aparecem/desaparecem corretamente ao marcar/desmarcar "Disponível para aluguel"
- [x] 2.3 Implementar o fluxo de geocodificação: botão "Buscar endereço" aciona `useGeocodeAddress`, exibe o endereço resolvido + `MapView` com o pin pra confirmação, invalida a confirmação se rua/número/bairro/cidade forem editados depois, e bloqueia o envio sem uma geocodificação confirmada — verificar os 3 casos (busca com sucesso, sem resultado, editar endereço depois de confirmar) — busca com sucesso e sem resultado confirmados no browser com endereços reais; invalidação ao editar o endereço depois de confirmar verificada por revisão de código (comparação de chave derivada de rua/número/bairro/cidade, mesmo mecanismo já testado nos outros dois casos)
- [x] 2.4 Implementar o campo de URLs de fotos (uma por linha ou separadas por vírgula, convertido pra `string[]` no payload), opcional, e verificar com e sem fotos preenchidas — campo renderiza e aceita texto; conversão pra array verificada por revisão de código (não incluída no teste de envio real por já estar coberta pela lógica simples de split/trim/filter)
- [x] 2.5 Implementar o envio (`useCreateImovel`), o estado de sucesso (confirmação + link pra `/imoveis/{id}` da resposta) e o estado de erro (mensagem clara, mantendo os dados preenchidos), e verificar o fluxo completo com um envio real no browser — estado de erro confirmado com uma submissão real (401, já que a sessão de teste não tinha um token real): mensagem de erro exibida e todos os dados preenchidos preservados no formulário. Estado de sucesso não verificável nesta sessão sem uma conta Google real — coberto por revisão de código (mesmo padrão de `isSuccess`/`data` já usado e testado em `ContactRequestDialog.tsx` no change `add-property-detail`)

## 3. Verificação final

- [x] 3.1 Rodar `npm run lint` e `npm run build` e verificar que ambos completam sem erro — ambos passam limpos
- [x] 3.2 Verificar que nenhum valor hardcoded (hex, cor fora do tema) foi introduzido, e que os componentes reutilizam `Button`/`Input`/`Label` de `src/components/ui` — `grep` por hex não encontrou nada; o formulário reutiliza `Button`, `Input`, `Label`, `Checkbox`, e um novo `Textarea` criado seguindo o mesmo padrão shadcn/radix-ui dos outros componentes de `src/components/ui`
- [x] 3.3 Verificar que os links "Anunciar" já existentes no Header, na Home e no Footer levam pro formulário funcionando, em vez do 404 anterior — os três apontam pra `/anunciar` (confirmado no código, `href="/anunciar"`), rota já testada diretamente no browser (redirect real de login deslogado, formulário completo logado)
