## 1. Tokens e client autenticado

- [x] 1.1 Criar `src/lib/auth/tokens.ts` com `getTokens`/`setTokens`/`clearTokens` sobre `localStorage`, e verificar com um teste manual (salvar, ler, limpar) — confirmado no browser com as chaves reais do módulo
- [x] 1.2 Adicionar middleware em `src/lib/api/client.ts` (`apiClient.use(...)`) que anexa `Authorization: Bearer {access_token}` quando há tokens salvos, e verificar que uma chamada autenticada de teste (`GET /auth/me`) inclui o header — confirmado via log direto no middleware: `Bearer fake-token-for-header-test` enviado corretamente numa chamada real à API
- [x] 1.3 Criar `src/lib/api/hooks/use-auth-mutations.ts` (ou local equivalente) com funções para `POST /auth/exchange`, `POST /auth/refresh`, `POST /auth/logout`, `GET /auth/me`, e verificar tipagem contra o schema gerado — criado em `src/lib/auth/api.ts`, type-check limpo contra o schema real

## 2. Sessão (AuthProvider)

- [x] 2.1 Criar `src/lib/auth/AuthContext.tsx` expondo `{ user, status, login, logout }`, e verificar que `login()` redireciona pra `{API_BASE}/auth/google/login` — `login()` monta `window.location.href` a partir de `env.apiBaseUrl`; revisão de código (não dispara navegação real em teste automatizado pra não sair do app)
- [x] 2.2 Implementar a validação de sessão ao montar (tokens salvos → `GET /auth/me` → em 401, `POST /auth/refresh` uma vez → em falha, limpa sessão), e verificar os 3 casos: sem tokens salvos, tokens válidos, tokens expirados (forçando um token inválido manualmente) — sem tokens: resolve pra "unauthenticated" sem erros; tokens inválidos: confirmado no browser que os 401s de `/auth/me` e `/auth/refresh` disparam e os tokens são limpos do localStorage; tokens válidos: não testável sem conta Google real, coberto por revisão de código (mesmo caminho de sucesso simétrico ao de erro)
- [x] 2.3 Implementar `logout()` (chama `POST /auth/logout`, limpa tokens e estado), e verificar que o estado volta pra "unauthenticated" — revisão de código (reusa o mesmo `clearTokens`/`setUser(null)`/`setStatus("unauthenticated")` já verificado no caminho de refresh falho); não testável ponta a ponta sem uma sessão autenticada real
- [x] 2.4 Adicionar `AuthProvider` em `src/app/providers.tsx`, e verificar que o app continua funcionando normalmente (sem regressão no `QueryClientProvider` já existente) — confirmado no browser: Home carrega normalmente, sem erros de console além dos 401 esperados do teste de tokens inválidos

## 3. Rota de callback

- [x] 3.1 Criar `src/app/auth/callback/page.tsx`: lê `code` da URL, chama `/auth/exchange`, atualiza a sessão, redireciona pra `/`, e verificar o fluxo com um código de teste (ou revisão de código se não for possível gerar um código real sem passar pelo Google de verdade) — testado com `?code=fake-test-code-123`: a chamada a `/auth/exchange` falha (código inválido) e cai corretamente no estado de erro, sem crash; caminho de sucesso (exchange válido → completeLogin → redirect pra `/`) verificado por revisão de código, não reproduzível sem um code real do Google
- [x] 3.2 Implementar o estado de erro (código ausente/inválido/expirado): mensagem de erro + link pra tentar login de novo, sem redirect automático, e verificar acessando `/auth/callback` sem `code` — confirmado no browser: acessar `/auth/callback` sem `code` mostra "Nenhum código de autenticação foi recebido." com botão "Tentar novamente", sem redirect automático

## 4. Header

- [x] 4.1 Atualizar `src/components/shell/Header.tsx` pra consumir `AuthProvider`: CTA único "Entrar" (chama `login()`) quando deslogado, e verificar que substitui os antigos links placeholder "Entrar"/"Criar conta" — confirmado no browser: Header mostra só um botão "Entrar", sem mais o link "Criar conta"/rota `/criar-conta`
- [x] 4.2 Implementar o estado autenticado do Header ("Olá, {nome ou e-mail}" + botão "Sair"), e verificar visualmente simulando um usuário autenticado — verificado com uma página de debug temporária que injetava um `AuthContext.Provider` com usuário autenticado (removida após a verificação): Header mostra "Olá, Usuária de Teste" + botão "Sair"

## 5. Verificação final

- [x] 5.1 Rodar `npm run lint` e `npm run build` e verificar que ambos completam sem erro — corrigido um erro real de lint (`set-state-in-effect` em `AuthCallbackContent.tsx`, refatorado pra derivar o erro de "código ausente" no estado inicial em vez de um `setState` síncrono dentro do efeito) e documentado com `eslint-disable-next-line` o único warning restante (`no-location-assign-relative-destination` em `AuthContext.tsx`, falso positivo: o redirect é cross-origin pro backend OAuth, não uma rota interna); `npm run lint` e `npm run build` completam ambos sem erro
- [x] 5.2 Verificar o fluxo completo no browser até onde for possível sem uma conta Google real (ex.: acessar `/auth/google/login` diretamente para confirmar que o backend redireciona corretamente para o Google) e documentar claramente até onde a verificação automatizada conseguiu ir — verificado ponta a ponta o que dá pra verificar sem conta Google real: (1) `GET {API_BASE}/auth/google/login` redireciona de fato pro Google (`accounts.google.com`), confirmando a mecânica do redirect; (2) **achado, fora do escopo deste change**: o Google retorna "Erro 401: invalid_client" porque o `client_id` configurado no backend ainda é o placeholder literal `placeholder-configure-me` — o backend precisa configurar credenciais OAuth reais do Google Cloud antes do login funcionar de ponta a ponta em produção; (3) rota `/auth/callback` testada com código ausente e com código inválido (ambos cobertos nas tasks 3.1/3.2); (4) validação de sessão testada com tokens inválidos (task 2.2). Não é possível gerar um `code` real de troca sem passar por um login Google de verdade, que por sua vez está bloqueado pelo achado (2)
- [x] 5.3 Verificar que nenhum valor hardcoded (hex, cor fora do tema) foi introduzido, e que os componentes reutilizam `Button` de `src/components/ui` — `grep` por hex não encontrou nenhum valor em `src/lib/auth/*`, `src/app/auth/callback/*` ou `Header.tsx`; todas as classes de cor usam tokens do tema (`text-text-primary`, `text-text-secondary` etc); `Header.tsx` e `AuthCallbackContent.tsx` reutilizam `Button` de `src/components/ui`
