## 1. Página e componente de item

- [x] 1.1 Criar `src/app/favoritos/page.tsx`: `status !== "authenticated"` → estado de carregamento (`status === "loading"`) ou dispara `login()` (`status === "unauthenticated"`), sem renderizar a lista, e verificar os 2 casos no browser — confirmado no browser real (não deslogado): acessar `/favoritos` sem sessão redireciona de fato pro `accounts.google.com`, sem mostrar nenhuma lista
- [x] 1.2 Implementar os estados de carregamento, erro e lista vazia (`useFavoritos`), e verificar os 3 casos — carregamento verificado visualmente (skeleton); erro e lista vazia verificados por revisão de código (mesmo padrão já usado e testado em outras listagens do projeto)
- [x] 1.3 Criar `src/app/favoritos/_components/FavoritoItem.tsx`: exibe dados da unidade sempre; quando há `imovel_atual`, exibe título/preço e o item é um link pro detalhe; quando não há, exibe indicação clara de "sem anúncio ativo" e não é navegável, e verificar visualmente os 2 casos (com um favorito real e revisão de código pro caso sem anúncio ativo, se não houver dado de seed nesse estado) — não há dado de seed com `imovel_atual: null`; os dois casos foram verificados no browser com uma página de debug temporária (`AuthContext.Provider` fake + cache do TanStack Query pré-populado com dois favoritos sintéticos, um com anúncio ativo e um sem, removida após o teste): ambos renderizam corretamente, incluindo o preço/título reais do caso com anúncio e a mensagem "Sem anúncio ativo no momento" do outro
- [x] 1.4 Adicionar o botão de remover em `FavoritoItem.tsx` usando `useToggleFavorito(unidade).toggle()`, como elemento irmão do link (não aninhado), e verificar que remover funciona com um favorito real e não interfere com a navegação do item quando há anúncio ativo — verificado na mesma página de debug: clicar em remover disparou a mutação real (confirmado pelos 401s no console, já que não há sessão real ali) sem alterar a URL da página, confirmando que não interfere com o link. A mecânica de otimismo/rollback da mutação em si já foi verificada a fundo no change `add-property-detail` (mesmo hook, reaproveitado sem alteração)

## 2. Header

- [x] 2.1 Adicionar o link "Favoritos" em `Header.tsx`, visível só quando `status === "authenticated"`, entre a navegação principal e a ação de conta, e verificar nos dois estados (autenticado/não autenticado) no browser — confirmado: Header real (deslogado) não mostra o link; Header com uma página de debug temporária simulando autenticado (removida após o teste) mostra "Favoritos" antes de "Olá, {nome}"

## 3. Verificação final

- [x] 3.1 Rodar `npm run lint` e `npm run build` e verificar que ambos completam sem erro — ambos passam limpos
- [x] 3.2 Verificar que nenhum valor hardcoded (hex, cor fora do tema) foi introduzido, e que os componentes reutilizam `Button`/estilos já existentes de `src/components/ui` — `grep` por hex não encontrou nada; `FavoritoItem` reaproveita `toPropertyDisplayData`/`formatArea` e a mesma linguagem visual de badge/card já usada no resto do site
- [x] 3.3 Verificar o fluxo completo no browser até onde for possível: favoritar um imóvel na página de detalhe, acessar `/favoritos` e ver o item lá, remover, e confirmar que sumiu — documentando claramente qualquer parte que não dê pra verificar de ponta a ponta (ex.: um favorito sem anúncio ativo, se não houver dado de seed nesse estado) — não verificável nesta sessão: o fluxo ponta a ponta com uma sessão Google real (sem conta disponível), coberto por revisão de código + testes isolados de cada peça (redirect de login real confirmado, renderização dos dois casos de `FavoritoItem` confirmada com dados sintéticos, mutação de remover confirmada disparando de verdade sem interferir na navegação, link do Header confirmado nos dois estados)
