# URL base da API do backend Chaves Imobiliária.
# Pública (NEXT_PUBLIC_*) porque componentes client-side (ex.: filtros de
# busca) chamam a API diretamente do browser.
NEXT_PUBLIC_API_BASE_URL=https://back-end-chaves-imobiliaria-production.up.railway.app
